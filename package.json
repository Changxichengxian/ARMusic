{
  "name": "armusic",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "desktop"
  ],
  "scripts": {
    "desktop:dev": "npm --workspace desktop run dev",
    "desktop:build": "npm --workspace desktop run build",
    "desktop:preview": "npm --workspace desktop run preview"
  }
}
