# ARMusic Desktop

桌面版播放器骨架，当前使用 React + Vite。后续可以套进 Tauri，变成真正的 Windows 桌面应用。

## 运行

```powershell
npm install
npm run dev
```

## 当前包含

- 音乐库列表的界面骨架。
- 播放控制的本地状态。
- 局域网同步面板。
- 同步计划的数据结构和对比函数。

真实本地文件扫描、音频播放、局域网服务还没有接入。
